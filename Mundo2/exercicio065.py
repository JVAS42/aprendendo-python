denominador = 0
soma = 0
continuar = ''

while continuar != 'N':
    num = int(input('Informe um valor: '))
    soma += num
    denominador += 1

    if denomina
        if menor > num:
            menor = num
        if maior < num:
            maior = num
    continuar = str(input('Deseja continuar[S/N]? ')).strip().upper()

print('Média entre os valores digitados é {}'.format(soma/denominador))
print('O maior valor digitado é {} e o menor valor digitado é {}'.format(maior, menor))
